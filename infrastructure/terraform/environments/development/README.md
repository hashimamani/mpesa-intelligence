Intentionally empty. Local development uses `docker-compose.yml` at the repo
root (Postgres, Redis, MinIO) instead of Terraform-managed AWS resources —
see [infrastructure/README.md](../../../README.md).
